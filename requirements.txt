# Production pipeline dependencies (data_cleaning -> feature_engineering ->
# impact_model -> resource_engine -> advisory_generator -> dashboard ->
# feedback_loop) plus the model_validation benchmark.
#
# These are the only packages the shipped system imports. The earlier
# repo state contained an unfiltered environment dump; this is the real list.

streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Dashboard map (Live Incident Feed / geospatial evidence view)
folium>=0.15
streamlit-folium>=0.20

# NOTE: the abandoned ensemble in experiments/ requires additional heavy
# packages (xgboost, lightgbm, catboost, optuna, category_encoders,
# imbalanced-learn, sentence-transformers). They are intentionally NOT listed
# here because that code is not part of the shipped solution. Install them
# manually only if you want to reproduce the experiment.
